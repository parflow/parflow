#!/bin/sh
##### These lines are for Moab
#MSUB -l nodes=1
#MSUB -l partition=atlas
#MSUB -l walltime=0:30:00
#MSUB -q pbatch
#MSUB -m be
#MSUB -V

##### These are shell commands
date
make check

echo 'Done'
