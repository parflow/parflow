# CrunchFlow + ParFlow run artifacts (regenerated by the tests).
# These patterns intentionally do NOT apply to correct_output/ (see below).
/*.out
/*.out.*
/*.pfidb
/*.dist
/*.log
/*.silo
/CrunchJunk*
/PestControl.ant
