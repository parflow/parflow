#!/bin/sh
#BHEADER***********************************************************************
# (c) 1995   The Regents of the University of California
#
# See the file COPYRIGHT_and_DISCLAIMER for a complete copyright
# notice, contact person, and disclaimer.
#
# $Revision: 1.1.1.1 $
#EHEADER***********************************************************************

#=============================================================================
#
# Configuration file for Alpha OSF1 systems
#
#=============================================================================

PFTOOLS_INCLUDE="-I/g/g16/smithsg/local/include -I/usr/local/apps/netcdf/include -I/usr/local/include"
PFTOOLS_LIBS="-lm -L/g/g16/smithsg/local/lib/ -ltk4.2 -ltcl7.6 -lX11 -lm"
PFTOOLS_DEF="-DTOOLS_BYTE_SWAP -DAMPS_SPLIT_FFILE"
PFTOOLS_MAKE="gmake"

#=============================================================================
#
# AVS
#
#=============================================================================
# Don't set this if you don't have AVS
PFAVS_PATH=$AVS_PATH
PFAVS_DEF="-DTOOLS_BYTE_SWAP"

#=============================================================================
#
# ParFlow
#
#=============================================================================

PF_MAKE="gmake -j 4"

MPIR_HOME=/usr/local/tools/mpich/mpi
export MPIR_HOME

#
# For DEC's compiler
# Need to define -std1 for the stupid DEC compiler to get ansi!

PF_NODE_DEBUG="-warnproto -g -trapuv -readonly_strings"

PF_NODE_OPT="-DDEC_ALPHA_COMPILER_HACK -assume noaccuracy_sensitive -tune host -O4 -Olimit 2000"
#PF_NODE_OPT="-assume noaccuracy_sensitive -O -Olimit 2000 "

#PF_NODE_DEF="-DAMPS_MPI_NOT_USE_PERSISTENT -DPF_MEMORY_ALLOC_CHECK -I$MPIR_HOME/include -DAMPS_PRINT_HOSTNAME -DCPUTiming -std1 -DAMPS_SPLIT_FFILE"
PF_NODE_DEF="-DPF_MEMORY_ALLOC_CHECK -I$MPIR_HOME/include -DAMPS_PRINT_HOSTNAME -DCPUTiming -std1 -DAMPS_SPLIT_FFILE"

PF_NODE_F77_OPT="-fast -O4 -tune host"

PF_NODE_LIBS="-L$MPIR_HOME/lib -lmpich -lrpc -lm -lgs"

