#!/bin/sh
#BHEADER***********************************************************************
# (c) 1995   The Regents of the University of California
#
# See the file COPYRIGHT_and_DISCLAIMER for a complete copyright
# notice, contact person, and disclaimer.
#
# $Revision: 1.1.1.1 $
#EHEADER***********************************************************************

#=============================================================================
#
# Configuration file for Alpha OSF1 systems
#
#=============================================================================

PFTOOLS_INCLUDE="-I/g/g16/smithsg/local.OSF1/include -I/usr/local/include"
PFTOOLS_DEF="-DTOOLS_BYTE_SWAP -DAMPS_SPLIT_FFILE"
PFTOOLS_SHLIB_LD="ld -shared -expect_unresolved \"*\""
PFTOOLS_MAKE="gmake"

#=============================================================================
#
# AVS
#
#=============================================================================
# Don't set this if you don't have AVS
PFAVS_PATH=$AVS_PATH
PFAVS_DEF="-DTOOLS_BYTE_SWAP"

#=============================================================================
#
# ParFlow
#
#=============================================================================

PF_MAKE="gmake -j 4"

#
# For DEC's compiler
# Need to define -std1 for the stupid DEC compiler to get ansi!

PF_NODE_DEBUG="-warnproto -g -trapuv -readonly_strings -DAMPS_STDOUT_NOBUFF "

PF_NODE_OPT="-DDEC_ALPHA_COMPILER_HACK -assume noaccuracy_sensitive -tune host -O4 -Olimit 2000"

PF_NODE_DEF="-DAMPS_MPI_PACKAGE_LOWSTORAGE -DPF_MEMORY_ALLOC_CHECK -DAMPS_PRINT_HOSTNAME -DCPUTiming -std1 -DAMPS_SPLIT_FFILE -pthread"

PF_NODE_F77_OPT="-fast -O4 -tune host -pthread"

PF_NODE_LIBS="-lmpi -lm -lots -lrt "


#
# Warning DEC MPI does not work with the dmalloc stuff
#
##
## If Malloc debug then link to the malloc debug library
##
#if [ -n "$DMALLOC_OPTIONS" ] 
#then
#    PF_NODE_DEF="$PF_NODE_DEF -DAMPS_MALLOC_DEBUG -I/g/g16/smithsg/local.OSF1/include"
#    PF_NODE_LIBS="$PF_NODE_LIBS -DAMPS_MALLOC_DEBUG -L/g/g16/smithsg/local.OSF1/lib -ldmalloc"
#fi
