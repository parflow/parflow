#!/bin/sh

file=$1
ext="${file##*.}"

case $ext in
   tcl)
      tclsh $@
      ;;

   py)
      pfpython $@
      ;;
   *)
      echo "Error <$file> is not a TCL or Python script"
      ;;
esac
     
