#SET(LATEX_OUTPUT_PATH build)
INCLUDE(UseLATEX)

file(GLOB_RECURSE PARFLOW_USER_MANUAL_FILES RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} *.tex *.sty)
file(GLOB_RECURSE PARFLOW_USER_MANUAL_IMAGES RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} *.pdf)

ADD_LATEX_DOCUMENT(
    user_manual.tex
    INPUTS ${PARFLOW_USER_MANUAL_FILES}
    IMAGES ${PARFLOW_USER_MANUAL_IMAGES}
    BIBFILES newRefs_ParFlowManual.bib
    USE_INDEX
    )

ADD_LATEX_DOCUMENT(
    dev_manual.tex
    INPUTS ${PARFLOW_USER_MANUAL_FILES}
    IMAGES ${PARFLOW_USER_MANUAL_IMAGES}
    )

configure_file (version.tex.in version.tex)
